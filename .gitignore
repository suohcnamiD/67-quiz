.DS_Store

# Generated artifacts (regenerated locally / in CI)
api/
frontend/src/api/

# Playwright CLI scratch
**/.playwright-cli/

# Local Claude Code settings
.claude/

# Backend test runner output
backend/test-results/
backend/build/

# Uploaded user files (avatars, etc.) — local dev only
uploads/
backend/uploads/

# Frontend bundle copied into the backend resources at build time.
# CI rebuilds it from frontend/dist; we don't check it in.
backend/src/main/resources/static/
frontend/dist/
