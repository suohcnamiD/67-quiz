info:
  name: Log in
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/authentication/login
  body:
    type: json
    data: |-
      {
        "username": "chovy123",
        "password": "chovy123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
