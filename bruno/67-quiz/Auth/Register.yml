info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/authentication/register
  body:
    type: json
    data: |-
      {
        "username": "loserloser",
        "password": "loserloser"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
